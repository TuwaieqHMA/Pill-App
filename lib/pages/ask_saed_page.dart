import 'package:flutter/material.dart';

class AskSaedPage extends StatelessWidget {
  const AskSaedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Ask Saed")),
    );
  }
}